<div id="card-container">
  {{#each cards}}
  <div class="card-div">
    <img class="cards" src={{this.imgPath}}>
    <a class="card-name" href=`/{{this.id}}`>{{this.name}}</a>
    <p class="card-price">{{this.price}}</p>
  </div>
  {{/each}}
</div>
